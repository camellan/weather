sudo: required
language: generic

services:
    - docker

script:
    - wget -O- https://raw.githubusercontent.com/harisvsulaiman/element-build/master/script.sh | sh -
    - bash -ex builders/appimage.sh || true

branches:
except:
    - /^debian\/\d/
