sudo: required
language: generic

services:
    - docker

deploy:
  skip_cleanup: true

script:
    - wget -O- https://raw.githubusercontent.com/harisvsulaiman/element-build/master/script.sh | sh -
    - bash -ex data/appimage.sh || true

branches:
except:
    - /^debian\/\d/
